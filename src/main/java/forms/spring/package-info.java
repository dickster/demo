/**
 * i should refactor all the @Configuration files...or maybe generate unit tests?
 * - there are some beans that are mandatory (but customizable).
 * - some you can't change
 * - some are optional (included in template but can be removed?)
 * - figure it out as number of beans grows...where to put them all.
 * 1:validators 2:ajaxBehaviors 3:formatters 4:states & workflows 5:utils 6:data providers
 */
package forms.spring;
